# Ustaad uses Gemini from a server route. Never expose this key in NEXT_PUBLIC variables.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# No database is required. Mill plants, assets, alerts, cases, and
# templates are hardcoded in src/lib/mill-data.ts and read in-browser.
